.DS_Store
node_modules
*.log

# server secrets and runtime state
server/.env
server/state.json
# Astro build artefacts (regenerated by `npm run build` in site/)
dist-astro/

# Astro project deps + cache
site/node_modules/
site/.astro/
site/dist/
